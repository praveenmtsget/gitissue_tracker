apiVersion: v1
kind: Service
metadata:
  name: {{ include "githubbot.fullname" . }}
  labels:
    {{- include "githubbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "githubbot.selectorLabels" . | nindent 4 }}
